
@model  IEnumerable<Roller.Models.Customer>



@{
    ViewBag.Title = "Customers";
    Layout = "~/Views/Shared/_Layout.cshtml";

}

@*@{
        var className = Model.Customers.Count > 1 ? "Popular" : null;

    }*@

<h1>Customers</h1>

@if (!Model.Any())
{
<text>We do not have any movies yet. </text>

}

else
{



<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Customers</th>
            <th>Discount Rate</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var customer in Model)
        {
        <tr>
            <td>@Html.ActionLink(customer.Name, "Edit", "Customer", new { id = customer.Id }, null)</td>
            <td>@customer.MembershipType.DiscountRate%</td>
        </tr>
        }
    </tbody>
</table>





}